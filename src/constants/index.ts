import { ROUTES } from './routes';

import {
  DEFAULT,
  APP,
  REGISTER,
  LOGIN,
  NOT_FOUND,
  DISPLAY_TYPE,
} from './content';

import { INPUT } from './inputs';
import { BUTTON } from './buttons';
import { SUCCESS } from './success';
import { ERRORS } from './errors';

import { STORE_MAIN_MODULE } from './modules';

import { ALERT, SIZE, LOADER } from '@/constants/style';

export {
  ROUTES,
  DEFAULT,
  APP,
  REGISTER,
  LOGIN,
  NOT_FOUND,
  INPUT,
  BUTTON,
  DISPLAY_TYPE,
  SUCCESS,
  ERRORS,
  STORE_MAIN_MODULE,
  LOADER,
};
